# Cleaning SaaS — FACTUAL PROJECT STATE (v4)

Формат:
✅ сделано 🟡 частично / в процессе ⛔ не делали

Документ фиксирует **только фактическое состояние проекта на сегодня**, без планов, гипотез и дорожных карт.

---

## 🧠 СЛОЙ 0 — ЯДРО (Backend + Manager Portal)

### Backend (Django, API-first)

**Core execution**

* Jobs (модель, связи, бизнес-логика) ✅
* Status flow: `scheduled → in_progress → completed` ✅
* Check-in / Check-out

  * GPS
  * distance validation ✅
* Checklist

  * JobChecklistItem
  * required items
  * toggle / bulk update ✅
* Photos before / after

  * EXIF extraction
  * distance validation
  * normalization to JPEG
  * storage + File model
  * связь с Job ✅
* Audit trail (JobCheckEvent) ✅
* PDF report

  * реальная генерация бинарного PDF
  * эндпоинт `/api/jobs/<id>/report/pdf/`
  * используется теми же данными, что UI ✅

👉 **Backend-ядро для job execution полностью закрыто.**

---

### Backend — Trial & Usage Layer

* Trial activation endpoint

  * `POST /api/cleanproof/trials/start/` ✅
  * включает 7-дневный trial для компании
  * повторный вызов в dev-среде безопасен
  * возвращает: `plan`, `trial_started_at`, `trial_expires_at`, `is_trial_active`, `is_trial_expired`, `days_left` ✅
* Trial state

  * истечение trial считается только на backend
  * `is_trial_active` / `is_trial_expired` / `days_left` — truth source ✅
* Usage summary endpoint

  * `GET /api/cleanproof/usage-summary/` ✅
  * возвращает агрегированное состояние:

    * план и trial-флаги
    * `jobs_today_count`, `jobs_today_soft_limit`
    * `cleaners_count`, `cleaners_soft_limit` ✅
  * используется фронтом только для UX-индикации

👉 Trial / usage-слой реализован как **источник истины для UI**, но не как система жёсткого enforcement.

---

### Manager Portal (Web)

**Jobs / Execution**

* Today Jobs (страница Jobs, данные с API) ✅
* Job Details (manager view) ✅

  * таймлайн из `check_events`, фото и чеклиста ✅
  * фото before / after из JobPhoto ✅
  * чеклист read-only + статус в таймлайне ✅
  * GPS check-in/out + “Open in Maps” ✅
  * Generate PDF — реальный PDF с backend ✅
  * Download PDF — без повторной генерации ✅
  * Email PDF — stub (API есть, письма не уходят) 🟡

---

### Job Planning (расширение поверх изначального MVP)

**Backend**

* `/api/manager/jobs/planning/?date=`
* агрегированные данные + proof-флаги ✅

**Frontend**

* таблица (дата, локация, клинер, время, статус) ✅
* proof of work (Before / Checklist / After) — унифицированный UI ✅
* фильтры:

  * дата ✅
  * статусы (multi-select) ✅
  * cleaner (All / конкретный) ✅
  * location (All / конкретная) ✅
* сайдпанель Job Details из Planning:

  * базовая информация
  * proof
  * Generate PDF
  * переход в Job Details ✅

---

### Create Job (UX-консолидация)

* backend: `/api/manager/meta/` + `/api/manager/jobs/` ✅
* frontend: форма создания job подключена к API ✅
* job появляется в Planning и Dashboard без перезагрузки ✅
* основной пользовательский сценарий создания job — **через Job Planning (side panel)** ✅
* standalone-страница `/create-job` технически существует, но **не используется как основной сценарий** 🟡
* кнопки Create Job в UI ведут в Job Planning, а не в отдельную страницу ✅

👉 Create Job **консолидирован вокруг Job Planning**. Отдельный экран не считается частью основного UX.

---

### Trial UX (Manager dashboard)

* Dashboard тянет `GET /api/cleanproof/usage-summary/` ✅
* Вверху Dashboard показывается trial-баннер, если:

  * `plan === "trial"` или `is_trial_active === true` ✅
* Баннер содержит:

  * статус trial
  * количество дней до окончания (`days_left`) ✅
  * спокойный текст без давления ✅
  * кнопку **Upgrade**, ведущую на страницу тарифов `/cleanproof/pricing` ✅
* При малом `days_left` текст становится более заметным, без тревожной раскраски и модалок ✅

👉 Trial UX в Dashboard реализован как **информационный слой**. Никаких блокировок действий.

---

👉 **Статус слоя 0: DONE ✅**
Ядро для исполнения работ + trial-индикации и usage-summary реализовано и связано с Manager Portal.

---

## 📱 СЛОЙ 1 — ИСПОЛНЕНИЕ (Mobile Cleaner App)

### Mobile App (React Native / Expo)

* Login

  * email / password
  * token auth
  * проверка роли cleaner ✅
* Today Jobs (`/api/jobs/today/`) ✅
* Job Detail

  * адрес
  * статус
  * чеклист
  * действия ✅
* Check-in (GPS, `/check-in/`) ✅
* Checklist

  * toggle
  * bulk update ✅
* Photos before / after

  * backend полностью готов ✅
  * frontend: камера + upload реализованы
  * были баги (ImagePicker, split of undefined)
  * сейчас работает, но требует чистки и стабилизации 🟡
* Check-out (GPS, `/check-out/`) ✅
* Job completed screen (минимальный) ✅
* Navigation to location

  * в web есть “Open in Maps” ✅
  * в mobile отдельная кнопка навигации не доведена ⛔

👉 **Статус слоя 1:**
Рабочий MVP. Основные риски — камера и навигация 🟡

---

## 🧑‍💼 СЛОЙ 2 — УПРАВЛЕНИЕ (Admin / Manager расширение)

### Locations

* backend модели и API — есть ✅
* locations используются при создании job (через `location_id`) ✅
* locations возвращаются через `/api/manager/meta/` (ограниченный список) ✅
* UI (list / create / edit) — реализован в базовом виде 🟡
* выявлена несогласованность:

  * новые локации не всегда появляются в селектах без перезагрузки 🟡
  * отсутствует единый источник истины для locations на фронте 🟡
* геокодинг и автопоиск адреса — частично 🟡

👉 Locations функциональны, но **frontend-синхронизация с backend неполная**.

---

### Checklist templates

* backend модели и snapshot-логика — есть ✅
* UI для управления шаблонами (CRUD) ⛔

---

### Jobs (manager-level)

* backend полностью готов ✅
* создание job через Planning — есть ✅
* редактирование существующих job — нет 🟡

---

### Назначение cleaners

* назначение при создании job — есть ✅
* массовое переназначение / отдельный UI — нет ⛔

---

### История jobs

* Today Jobs (cleaner) — есть ✅
* Planning по конкретной дате — есть ✅
* отдельная History-страница с диапазонами дат — нет 🟡

---

### Фильтры и поиск

* дата ✅
* статусы (multi-select) ✅
* cleaner ✅
* location ✅
* текстовый поиск / диапазоны дат — нет ⛔

---

### Повторяющиеся jobs

* recurring / шаблоны расписаний ⛔

---

### Company Settings & Team (Manager Portal)

**Company Settings (UI)**

* Страница Settings реализована ✅
* Company info:

  * Company Name (editable, сохраняется в backend) ✅
  * Contact Email / Phone (editable, сохраняются в backend) ✅
* Company logo:

  * загрузка файла через hidden `<input type="file">` ✅
  * превью логотипа (client-side preview) ✅
  * фактическая отправка логотипа на backend 🟡

**Team / Cleaners (UI)**

* Блок Team / Cleaners на Settings-странице ✅
* Список клинеров (реальные данные из backend) ✅

  * имя
  * email
  * телефон
  * статус Active / Inactive
* Добавление cleaner через modal (POST API) ✅
* Изменение Active / Inactive (PATCH API) ✅

**Soft-limits UX для cleaners**

* Settings тянет `usage-summary` ✅
* На основе `cleaners_count` и `cleaners_soft_limit` показывается soft-limit баннер ✅
* Никаких блокировок создания cleaner нет ✅
* В баннере Upgrade ведёт на `/cleanproof/pricing` ✅

👉 **Статус слоя 2:**
Manager-level функциональность рабочая.
Company Settings и Team **подключены к реальному backend API**.
Основной технический долг — **Locations frontend-consistency** 🟡

---

## 💳 СЛОЙ 3 — КОММЕРЦИЯ

* Регистрация компаний (self-serve signup) ⛔
* Тарифы (backend enforcement, реальные ограничения) ⛔
* Trial enforcement (жёсткие лимиты, блокировки) ⛔
* Stripe / Paddle интеграция ⛔

**Что есть по факту**

* Trial backend: запуск, статус, истечение, `days_left` ✅
* Usage-summary: план + счётчики + soft-limits ✅
* Trial UX в Dashboard и Settings ✅
* Реальных блокировок по тарифам нет ✅

---

## 🌍 СЛОЙ 4 — МАРКЕТИНГ

* Landing (CleanProof) ✅
* Demo request page ✅
* Pricing page ✅
* Product updates page ✅
* Contact page ✅

---

## 📊 СЛОЙ 5 — МАСШТАБ

* Аналитика ⛔
* SLA / Exception management ⛔
* Performance reports ⛔
* Multi-company roles ⛔
* Audit exports ⛔

---

## ⚠️ Known limitations (as of today)

* Нет self-serve signup
* Нет биллинга и enforcement
* Contact / Demo формы не сохраняют данные
* Mobile camera UX требует стабилизации
* Upload логотипа компании — частично реализован
* Soft-limits — только UX-индикация
* Locations не полностью синхронизированы во всех фронтовых селектах

---

## Итог — коротко и честно

* Слой 0 — полностью закрыт ✅
* Слой 1 — рабочий MVP, но с рисками 🟡
* Слой 2 — менеджерский уровень подключён к backend, основной долг — Locations 🟡
* Слои 3–5 — не реализованы

👉 **Проект перешёл из “прототипа” в устойчивый операционный продукт без биллинга.**
