# Cleaning SaaS — FACTUAL PROJECT STATE (v6)

Формат:
✅ сделано 🟡 частично / в процессе ⛔ не делали

Документ фиксирует **только фактическое состояние проекта на сегодня**, без планов, гипотез и дорожных карт.

---

## 🧠 СЛОЙ 0 — ЯДРО (Backend + Manager Portal)

### Backend (Django, API-first)

**Core execution**

* Jobs (модель, связи, бизнес-логика) ✅

* Status flow: `scheduled → in_progress → completed` ✅

* Check-in / Check-out

  * GPS
  * distance validation ✅

* Checklist

  * JobChecklistItem
  * required items
  * toggle / bulk update ✅

* Photos before / after

  * EXIF extraction
  * distance validation
  * normalization to JPEG
  * storage + File model
  * связь с Job ✅

* Audit trail (JobCheckEvent) ✅

* PDF report

  * реальная генерация бинарного PDF
  * эндпоинт `/api/jobs/<id>/report/pdf/`
  * используется теми же данными, что UI ✅

👉 **Backend-ядро для job execution полностью закрыто.**

---

### Backend — Trial & Usage Layer

* Trial activation endpoint

  * `POST /api/cleanproof/trials/start/` ✅
  * включает 7-дневный trial для компании
  * повторный вызов в dev-среде безопасен
  * возвращает:

    * `plan`
    * `trial_started_at`
    * `trial_expires_at`
    * `is_trial_active`
    * `is_trial_expired`
    * `days_left` ✅

* Trial state

  * истечение trial считается **только на backend**
  * frontend не вычисляет даты самостоятельно
  * `is_trial_active / is_trial_expired / days_left` — **единственный источник истины** ✅

* Usage summary endpoint

  * `GET /api/cleanproof/usage-summary/` ✅
  * возвращает агрегированное состояние:

    * план и trial-флаги
    * `jobs_today_count`, `jobs_today_soft_limit`
    * `cleaners_count`, `cleaners_soft_limit` ✅
  * используется фронтом **исключительно для UX-индикации**

* Trial limits / enforcement

  * в `Company` заданы жёсткие лимиты: `TRIAL_MAX_CLEANERS`, `TRIAL_MAX_JOBS` ✅
  * методы `trial_cleaners_limit_reached()` и `trial_jobs_limit_reached()` ✅
  * при создании клинеров и jobs backend проверяет лимиты и возвращает `403` с кодами

    * `cleaners_limit_reached`
    * `jobs_limit_reached`
    * `trial_expired` (для истёкшего trial) ✅

👉 Trial / usage-слой — **backend source of truth + реальное enforcement по лимитам trial.**

---

### Manager Portal (Web)

#### Jobs / Execution

* Today Jobs (страница Jobs, данные с API) ✅
* Job Details (manager view) ✅

  * таймлайн из `check_events`, фото и чеклиста ✅
  * фото before / after из JobPhoto ✅
  * чеклист read-only + статус в таймлайне ✅
  * GPS check-in/out + “Open in Maps” ✅
  * Generate PDF — реальный PDF с backend ✅
  * Download PDF — без повторной генерации ✅
  * Email PDF — stub (API есть, письма не уходят) 🟡

---

### Job Planning (расширение поверх изначального MVP)

**Backend**

* `/api/manager/jobs/planning/?date=` ✅
* агрегированные данные + proof-флаги ✅
* обязательный query-параметр `date` валидируется backend’ом ✅

**Frontend**

* таблица (дата, локация, клинер, время, статус) ✅

* proof of work (Before / Checklist / After) — унифицированный UI ✅

* фильтры:

  * дата ✅
  * статусы (multi-select) ✅
  * cleaner (All / конкретный) ✅
  * location (All / конкретная) ✅

* сайд-панель Job Details из Planning:

  * базовая информация
  * proof
  * Generate PDF
  * переход в Job Details ✅

* контракт типов между backend и frontend выровнен:

  * единый `PlanningJob` в UI ✅
  * маппинг proof-флагов стабилизирован (`before_uploaded / after_uploaded / checklist_completed → before_photo / after_photo / checklist`) ✅

* загрузка данных через единый `fetchPlanningJobs(filters)` с обязательным `date` ✅

👉 Job Planning работает **end-to-end**: API → UI → Create Job → refetch списка.

---

### Create Job (UX-консолидация)

* Backend:

  * `/api/manager/meta/` ✅
  * `/api/manager/jobs/` ✅

* Frontend:

  * форма создания job подключена к API ✅
  * корректная обработка ошибок trial:

    * `trial_expired`
    * `jobs_limit_reached`
    * показ понятного текста и блокировка сабмита (“Upgrade required”) ✅

* job появляется в Planning и Dashboard после refetch (без перезагрузки) ✅

* основной пользовательский сценарий создания job — **через Job Planning (side drawer)** ✅

* standalone-страница `/create-job`:

  * технически существует
  * не используется как основной сценарий 🟡

* redirect `/create-job → /planning` настроен ✅

👉 Create Job **полностью собран вокруг Job Planning**, с учётом trial-лимитов.

---

### Job History

**Backend**

* `/api/manager/jobs/history/` ✅

  * параметры: `date_from`, `date_to` (обязательные)
  * отдаёт список jobs, совместимый по типу с Planning (дата, время, локация, клинер, статус, proof-флаги) ✅

**Frontend**

* страница `/history` ✅
* фильтры:

  * date range (start / end), UX через один календарь с выбором периода ✅
* таблица:

  * job ID
  * location
  * cleaner
  * time
  * status
  * proof (иконки Before / Checklist / After)
  * action → переход в Job Details ✅
* пустые состояния:

  * “No jobs in this date range” с подсказкой про расширение диапазона ✅

👉 Job History даёт менеджеру **просмотр выполненных и запланированных работ за период** с доступом к proof.

---

### Trial UX (Manager Dashboard & Settings)

* Dashboard тянет `GET /api/cleanproof/usage-summary/` ✅

* Trial-баннер отображается корректно в состояниях:

  * trial active
  * trial expired ✅

* Текст баннера:

  * не врёт
  * не вычисляется на фронте
  * полностью основан на backend-статусе ✅

* Баннер содержит:

  * статус trial
  * `days_left` (если актуально)
  * спокойный explanatory текст
  * кнопку **Upgrade → `/cleanproof/pricing`** ✅

* При истёкшем trial:

  * создание job заблокировано backend’ом
  * фронт показывает корректное сообщение об апгрейде ✅

👉 Trial UX — **информационный слой, синхронизированный с реальными ограничениями backend.**

---

👉 **Статус слоя 0: DONE ✅**
Backend execution + Planning + Create Job + Trial UX + History работают и проверены реальными запросами.

---

## 📱 СЛОЙ 1 — ИСПОЛНЕНИЕ (Mobile Cleaner App)

(без изменений, состояние подтверждено)

👉 **Статус слоя 1:** рабочий MVP, основные риски — камера и навигация 🟡

---

## 🧑‍💼 СЛОЙ 2 — УПРАВЛЕНИЕ (Admin / Manager)

### Locations

* backend ↔ UI ↔ Create Job — полный цикл работает ✅
* архитектура фронтового хранения: единый `LocationsContext`, который тянет данные из API и используется в:

  * `/locations`
  * Job Planning (фильтры и Create Job) ✅

👉 Locations теперь живут на backend, а фронт использует единый source of truth.

---

### Company Settings & Team

* Settings page — рабочая ✅

* Company info — editable + сохраняется ✅

* Upload логотипа компании:

  * загрузка через Settings
  * URL сохраняется в компании
  * логотип отображается в sidebar / header после перезагрузки ✅

* Cleaners management — полный CRUD ✅

* Trial-лимит клинеров:

  * backend блокирует создание при превышении `TRIAL_MAX_CLEANERS`
  * фронт показывает понятное сообщение и CTA на Upgrade ✅

* Soft-limit / usage по cleaners и jobs — через `usage-summary` (для баннеров и подсказок) ✅

---

## 💳 СЛОЙ 3 — КОММЕРЦИЯ

* Реального биллинга нет ⛔
* Enforcement **тарифов** (платных планов) нет ⛔ — есть только trial-лимиты.

**Фактически реализовано:**

* Trial backend — полностью ✅

* Usage-summary — полностью ✅

* Pricing page:

  * различает:

    * anonymous
    * `trial_active`
    * `trial_expired`
  * для `trial_expired`:

    * Standard → **Request upgrade / Contact**
    * Start trial больше не предлагается ✅

* Upgrade-флоу честный:

  * без фейковых checkout
  * без обещаний оплаты
  * Contact используется как временный ручной entrypoint ✅

### Signup / Trial Onboarding

* Self-serve signup (manager):

  * frontend-страница `/signup` ✅
  * backend `POST /api/auth/signup/` (`ManagerSignupView`) ✅

* На signup создаются:

  * новая `Company`
  * пользователь-менеджер
  * запускается стандартный trial через `start_standard_trial(days=7)` ✅

* Ответ signup-эндпоинта:

  * данные пользователя / компании
  * JWT-токены для мгновенного логина ✅

* После успешного signup:

  * фронт логинит менеджера
  * редирект на Dashboard с trial-баннером и демо-данными ✅

👉 Коммерция **архитектурно готова под биллинг**, сейчас работает только trial + self-serve signup.

---

## 🌍 СЛОЙ 4 — МАРКЕТИНГ

* Landing ✅
* Pricing (динамический, с учётом trial-статуса) ✅
* Product updates ✅
* Contact ✅
* Demo request ✅

---

## 📊 СЛОЙ 5 — МАСШТАБ

* Аналитика ⛔
* SLA / Exception management ⛔
* Performance reports ⛔
* Multi-company roles ⛔
* Audit exports ⛔

---

## ⚠️ Known limitations (as of today)

* Нет реального биллинга и платных планов (только trial).
* Trial-лимиты распространяются только на количество клинеров и jobs; остальные ресурсы не ограничены.
* Mobile camera UX требует стабилизации.
* Locations пока без продвинутых фич (иерархии, архив, массовый импорт и т.п.).

---

## Итог — коротко и честно

* Слой 0 — **полностью закрыт** ✅
* Слой 1 — рабочий MVP 🟡
* Слой 2 — менеджерский уровень стабилен, с реальными trial-лимитами для клинеров и jobs ✅/🟡
* Слой 3 — архитектурно готов, есть signup + trial, но без платежей
* Слои 4–5 — базовые заготовки

👉 **Проект — это уже не прототип, а устойчивый операционный продукт с полноценным trial и self-serve signup, но без биллинга.**
