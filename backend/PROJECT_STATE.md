# Cleaning SaaS — FACTUAL PROJECT STATE (v6)

Формат:
✅ сделано 🟡 частично / в процессе ⛔ не делали

Документ фиксирует **только фактическое состояние проекта на сегодня**, без планов, гипотез и дорожных карт.

---

## 🧠 СЛОЙ 0 — ЯДРО (Backend + Manager Portal)

### Backend (Django, API-first)

**Core execution**

* Jobs (модель, связи, бизнес-логика) ✅
* Status flow: `scheduled → in_progress → completed` ✅
* Check-in / Check-out

  * GPS
  * distance validation ✅
* Checklist

  * JobChecklistItem
  * required items
  * toggle / bulk update ✅
* Photos before / after

  * EXIF extraction
  * distance validation
  * normalization to JPEG
  * storage + File model
  * связь с Job ✅
* Audit trail (JobCheckEvent) ✅
* PDF report

  * реальная генерация бинарного PDF
  * эндпоинт `/api/jobs/<id>/report/pdf/`
  * используется теми же данными, что UI ✅

👉 **Backend-ядро для job execution полностью закрыто.**

---

### Backend — Trial & Usage Layer

* Trial activation endpoint

  * `POST /api/cleanproof/trials/start/` ✅
  * включает 7-дневный trial для компании
  * повторный вызов в dev-среде безопасен
  * возвращает:

    * `plan`
    * `trial_started_at`
    * `trial_expires_at`
    * `is_trial_active`
    * `is_trial_expired`
    * `days_left` ✅

* Trial state

  * истечение trial считается **только на backend**
  * frontend не вычисляет даты самостоятельно
  * `is_trial_active / is_trial_expired / days_left` — **единственный источник истины** ✅

* Usage summary endpoint

  * `GET /api/cleanproof/usage-summary/` ✅
  * возвращает агрегированное состояние:

    * план и trial-флаги
    * `jobs_today_count`, `jobs_today_soft_limit`
    * `cleaners_count`, `cleaners_soft_limit` ✅
  * используется фронтом **исключительно для UX-индикации**

* Trial limits / enforcement

  * в `Company` заданы жёсткие лимиты: `TRIAL_MAX_CLEANERS`, `TRIAL_MAX_JOBS` ✅
  * методы `trial_cleaners_limit_reached()` и `trial_jobs_limit_reached()` ✅
  * при создании клинеров и jobs backend проверяет лимиты и возвращает `403` с кодами:

    * `cleaners_limit_reached`
    * `jobs_limit_reached`
    * `trial_expired` ✅

👉 Trial / usage-слой — **backend source of truth + реальное enforcement по лимитам trial.**

---

### Manager Portal (Web)

#### Jobs / Execution

* Today Jobs (страница Jobs, данные с API) ✅
* Job Details (manager view) ✅

  * таймлайн из `check_events`, фото и чеклиста ✅
  * фото before / after из JobPhoto ✅
  * чеклист read-only + статус в таймлайне ✅
  * GPS check-in/out + “Open in Maps” ✅
  * Generate PDF — реальный PDF с backend ✅
  * Download PDF — без повторной генерации ✅
  * Email PDF — **реальная отправка PDF на email менеджера** ✅
    * endpoint: `POST /api/manager/jobs/<id>/report/email/`
    * PDF генерируется тем же backend-кодом, что и Download PDF (single source of truth)
    * email по умолчанию берётся из `request.user.email`
    * допускается передача кастомного email в теле запроса (dev / v1)
    * UI-кнопка “Email PDF” подключена к backend
    * подтверждение / ошибка отображаются inline (без alert-ов)

---

### Job Planning (расширение поверх изначального MVP)

**Backend**

* `/api/manager/jobs/planning/?date=` ✅
* агрегированные данные + proof-флаги ✅
* обязательный query-параметр `date` валидируется backend’ом ✅

**Frontend**

* таблица (дата, локация, клинер, время, статус) ✅
* proof of work (Before / Checklist / After) — унифицированный UI ✅
* фильтры:

  * дата ✅
  * статусы (multi-select) ✅
  * cleaner (All / конкретный) ✅
  * location (All / конкретная) ✅
* сайд-панель Job Details из Planning:

  * базовая информация
  * proof
  * Generate PDF
  * переход в Job Details ✅
* контракт типов между backend и frontend выровнен:

  * единый `PlanningJob` в UI ✅
  * маппинг proof-флагов стабилизирован (`before_uploaded / after_uploaded / checklist_completed → before_photo / after_photo / checklist`) ✅
* загрузка данных через единый `fetchPlanningJobs(filters)` с обязательным `date` ✅

👉 Job Planning работает **end-to-end**: API → UI → Create Job → refetch списка.

---

### Create Job (UX-консолидация)

* Backend:

  * `/api/manager/meta/` ✅
  * `/api/manager/jobs/` ✅
* Frontend:

  * форма создания job подключена к API ✅
  * корректная обработка ошибок trial:

    * `trial_expired`
    * `jobs_limit_reached`
    * показ понятного текста и блокировка сабмита (“Upgrade required”) ✅
* job появляется в Planning и Dashboard после refetch (без перезагрузки) ✅
* основной пользовательский сценарий создания job — **через Job Planning (side drawer)** ✅
* standalone-страница `/create-job`:

  * технически существует
  * не используется как основной сценарий 🟡
* redirect `/create-job → /planning` настроен ✅

👉 Create Job **полностью собран вокруг Job Planning**, с учётом trial-лимитов.

---

### Job History

**Backend**

* `/api/manager/jobs/history/` ✅

  * параметры: `date_from`, `date_to` (обязательные)
  * отдаёт список jobs, совместимый по типу с Planning (дата, время, локация, клинер, статус, proof-флаги) ✅

---

### Performance Layer (SLA aggregation v1.5)

**Backend**

* `/api/manager/performance/?date_from=&date_to=` ✅
* агрегирует SLA-нарушения по клинерам и локациям
* учитываются только `completed jobs` за период и компанию менеджера
* вычисляются:
  * `jobs_total`
  * `jobs_with_sla_violations`
  * `violation_rate` (violations / jobs_total)
  * `has_repeated_violations` (≥2 нарушений с одинаковым SLA reason-code) ✅
* SLA reports aggregation endpoints:
  * `/api/manager/reports/weekly/` ✅
  * `/api/manager/reports/monthly/` ✅
  * агрегируют данные на основе Performance + Job History:
    * jobs_count
    * violations_count
    * issue_rate
    * топ клинеров с нарушениями
    * топ локаций с нарушениями
    * top SLA reason-codes
  * учитываются только jobs текущей компании менеджера
  * период считается строго на backend (7 / 30 дней) ✅
* Reports PDF export:
  * endpoints:
    * `/api/manager/reports/weekly/pdf/` ✅
    * `/api/manager/reports/monthly/pdf/` ✅
  * используется та же агрегированная логика, что и UI (single source of truth) ✅
  * PDF генерируется backend’ом (ReportLab)
  * содержит:
    * название компании
    * период отчёта
    * summary по SLA
    * таблицы клинеров и локаций
    * top SLA reasons
  * логотип компании:
    * upload логотипа в Company работает ✅
    * отображение логотипа в PDF — **не реализовано** (осознанно отложено) 🟡
* Reports email:
  * endpoints:
    * `/api/manager/reports/weekly/email/` ✅
    * `/api/manager/reports/monthly/email/` ✅
  * принимают JSON с опциональным полем `email`
  * если email не передан — используется `request.user.email`
  * формируют тот же PDF, что и `.../pdf/`
  * выполняют реальную отправку письма (dev: `locmem`, prod — настраивается через `EMAIL_BACKEND`) ✅

**Frontend**

* страница `/performance` ✅
* раздельные списки:
  * Cleaners — зона ответственности
  * Locations — операционный риск
* отображаются относительные показатели (`violation_rate`)
* повторяемые проблемы помечаются бейджем (Recurring / Repeated issues)
* экран read-only, без графиков и скорингов
* Reports UI:
  * страница `/reports` ✅
  * переключение Weekly / Monthly (frontend state only) ✅
  * summary-блок:
    * период
    * jobs count
    * SLA violations count
    * issue rate (%) ✅
  * таблицы:
    * Cleaners with issues (jobs / violations)
    * Locations with issues (jobs / violations)
  * Top SLA reasons — список reason-codes с подсчётом ✅
  * навигация:
    * “View jobs” ведёт в Job History с предустановленными фильтрами (date range + cleaner / location) ✅
* Reports PDF export:
  * кнопка **Download PDF** подключена к `/weekly/pdf` / `/monthly/pdf` ✅
* Reports email:
  * кнопка **Email report** подключена к `/weekly/email` / `/monthly/email` ✅
  * в v1 отправляет отчёт на email текущего менеджера (без выбора адреса)
  * успешная отправка / ошибка отображаются в UI (inline-текст под кнопкой) ✅

👉 Performance Layer усиливает SLA и замыкает цепочку:
**Execution → SLA → Performance → History → Evidence**

---

### Trial UX (Manager Dashboard & Settings)

* Dashboard тянет `GET /api/cleanproof/usage-summary/` ✅
* Trial-баннер отображается корректно в состояниях:

  * trial active
  * trial expired ✅
* Текст баннера:

  * не врёт
  * не вычисляется на фронте
  * полностью основан на backend-статусе ✅
* Баннер содержит:

  * статус trial
  * `days_left` (если актуально)
  * спокойный explanatory текст
  * кнопку **Upgrade → `/cleanproof/pricing`** ✅
* При истёкшем trial:

  * создание job заблокировано backend’ом
  * фронт показывает корректное сообщение об апгрейде ✅

👉 Trial UX — **информационный слой, синхронизированный с реальными ограничениями backend.**

---

👉 **Статус слоя 0: DONE ✅**

---

## 📱 СЛОЙ 1 — ИСПОЛНЕНИЕ (Mobile Cleaner App)

(без изменений, состояние подтверждено)

👉 **Статус слоя 1:** рабочий MVP, основные риски — камера и навигация 🟡

---

## 🧑‍💼 СЛОЙ 2 — УПРАВЛЕНИЕ (Admin / Manager)

### Locations

* backend ↔ UI ↔ Create Job — полный цикл работает ✅
* архитектура фронтового хранения: единый `LocationsContext` используется в:

  * `/locations`
  * Job Planning (фильтры и Create Job) ✅

---

### Company Settings & Team

* Settings page — рабочая ✅
* Company info — editable + сохраняется ✅
* Upload логотипа компании — полный цикл работает ✅
* Cleaners management — полный CRUD ✅
* Trial-лимит клинеров — backend enforcement + UX-индикация ✅
* Soft-limit / usage по cleaners и jobs — через `usage-summary` ✅

---

## 💳 СЛОЙ 3 — КОММЕРЦИЯ

* Реального биллинга нет ⛔
* Enforcement тарифов нет ⛔ (есть только trial-лимиты)

👉 Коммерция архитектурно готова, работает в режиме trial + contact-upgrade.

---

## 🌍 СЛОЙ 4 — МАРКЕТИНГ

* Landing ✅
* Pricing (динамический) ✅
* Product updates ✅
* Contact ✅
* Demo request ✅

### Marketing intake (backend-connected)

* Demo request form (`/cleanproof/demo`) ✅
  * frontend форма подключена к backend
  * endpoint: `POST /api/public/demo-requests/`
  * данные сохраняются в БД (`DemoRequest`)
  * отправляется email-уведомление фаундеру
  * поля v1:
    * company name
    * role
    * cleaners count
    * contact
    * country
    * primary pain

* Contact form (`/cleanproof/contact`) ✅
  * frontend форма подключена к backend
  * endpoint: `POST /api/public/contact-messages/`
  * сообщения сохраняются в БД (`ContactMessage`)
  * отправляется email-уведомление
  * форма **не является заглушкой**, работает end-to-end

👉 Marketing-слой — **не статический**, а реально подключён к backend.

---

## 📊 СЛОЙ 5 — МАСШТАБ

* Аналитика ⛔
* SLA / Exception management 🟡
* Performance aggregation (SLA-based) ✅
* Performance reports (UI + PDF + Email, SLA-based, manager-ready) ✅
* Multi-company roles ⛔
* Audit exports ⛔

---

## ⚠️ Known limitations (as of today)

* Нет реального биллинга и платных планов.
* Trial-лимиты действуют только на jobs и cleaners.
* Mobile camera UX требует стабилизации.
* Locations без продвинутых фич.
* Для отчётов (job / reports) нет истории отправок и выбора email — текущий v1 отправляет только на адрес менеджера или на явно переданный email.

---

## Итог — коротко и честно

* Слой 0 — полностью закрыт ✅
* Слой 1 — рабочий MVP 🟡
* Слой 2 — менеджерский уровень стабилен ✅
* Слой 3 — готов к биллингу, но без платежей
* Слои 4–5 — базовые заготовки, но Performance / Reports уже дают управленческую ценность ✅

👉 **Проект — устойчивый операционный продукт с реальным контролем качества (micro-SLA v1 + reason codes), trial-онбордингом и self-serve signup, но без биллинга.**
