MASTER BRIEF — Cleaning SaaS (MVP)
1. Что это за проект

Cleaning SaaS — backend-first SaaS для клининговых компаний (рынок UAE).

Проект покрывает реальный операционный цикл:

Планирование → Исполнение → Контроль → Отчёт

Фокус MVP

дисциплина и прозрачность работы клинеров

контроль выполнения через:

GPS

чек-листы

фото before / after

PDF-отчёт как финальный, юридически понятный артефакт работы

Проект не про биллинг, маркетинг или CRM.
Только операционка.

2. Архитектура (ЗАФИКСИРОВАНА)
Backend

Django (API-first)

DRF — только как API-слой

Auth: Token (TokenAuthentication)

БД:

SQLite (локально)

схема спроектирована под PostgreSQL

Асинхронщины нет

Очередей нет

Всё синхронно, прозрачно и детерминировано

Роли

Manager

Cleaner

3. Структура проекта (фактически есть)
backend/
├── apps/
│   ├── accounts/
│   │   └── models.py        # User, Company, roles
│   ├── api/
│   │   ├── views.py         # DRF API (login, jobs, checklist, photos, pdf)
│   │   ├── urls.py
│   │   ├── serializers.py
│   │   └── pdf.py           # генерация PDF отчёта
│   ├── jobs/
│   │   ├── models.py        # Job, JobChecklistItem, JobCheckEvent, File, JobPhoto
│   │   ├── utils.py         # distance_m, extract_exif_data
│   │   ├── migrations/
│   │   └── admin.py
│   └── locations/
│       └── models.py        # Location, ChecklistTemplate, ChecklistTemplateItem
├── config/
│   └── urls.py
├── requirements.txt
└── manage.py

Важные правила

apps.api — единственный слой API

apps.jobs.views — legacy, не расширять

Новая логика → apps.api

Passing tests = истина

4. Что реализовано (DONE)
✅ Аутентификация

POST /api/auth/login/

email + password

Token-based auth

логин только для active cleaner

проверено через curl

✅ Jobs (Cleaner flow)

GET /api/jobs/today/

GET /api/jobs/<id>/

Статусы job:

scheduled

in_progress

completed

✅ Check-in / Check-out

POST /api/jobs/<id>/check-in/

POST /api/jobs/<id>/check-out/

Логика:

GPS проверка (≤ 100 м)

проверка роли

проверка владельца job

корректная смена статусов

создание JobCheckEvent

✅ Checklist

snapshot чек-листа при создании job

JobChecklistItem привязан к job

bulk update чек-листа

защита:

только назначенный клинер

только когда job = in_progress

required-пункты проверяются при check-out

✅ Photos (Phase 9 — ПОЛНОСТЬЮ РЕАЛИЗОВАНА)

Endpoint:

GET /api/jobs/<id>/photos/

POST /api/jobs/<id>/photos/

DELETE /api/jobs/<id>/photos/<photo_type>/

Правила:

ровно 2 фото на job:

before

after

загружает только клинер

только когда job = in_progress

after нельзя загрузить без before

нельзя удалить before, если уже есть after

Хранилище:

Django MEDIA_ROOT

путь:

/media/company/<company_id>/jobs/<job_id>/photos/<type>/<uuid>.<ext>

EXIF:

извлекаются:

latitude

longitude

timestamp

если EXIF есть:

проверка расстояния до location (≤ 100 м)

если EXIF нет:

upload разрешён

exif_missing = true в ответе

✅ PDF отчёт

POST /api/jobs/<id>/report/pdf/

генерация через ReportLab

PDF содержит:

job

location

cleaner

scheduled / actual timestamps

checklist

audit events

(фото готовы к включению в следующей итерации PDF)

Файл:

корректно скачивается

валиден

проверен вручную

✅ Tests

python manage.py test — OK

покрыты:

photos upload / rules / delete

order before → after

статусные ограничения

system check — OK

миграции применены

5. Что было сделано в этом чате (зафиксировано)

Полностью реализована Phase 9: Photos

Добавлены модели:

File

JobPhoto

Реализован EXIF extraction + GPS validation

Исправлены edge cases:

file pointer после EXIF

порядок before / after

delete-ограничения

Добавлены и доведены тесты до зелёного состояния

Актуализирован PROJECT_STATE.md

API реально прогнан через curl

Backend готов к подключению фронта

6. Текущее состояние проекта

MVP backend завершён функционально

API стабилен

Все core-флоу закрыты

PDF — финальный артефакт job

Проект можно:

показывать клиенту

подключать mobile / web frontend

использовать как основу подписочного SaaS

7. Что НЕ делать ❌

Запрещено:

переписывать auth

переписывать jobs

переписывать check-in / check-out

менять модели

трогать миграции

предлагать Celery / async / очереди

«рефакторить ради красоты»

Любые изменения — только через проверку тестами.

8. Что делать в СЛЕДУЮЩЕМ ЧАТЕ (TODO)
🔹 Dev-config для фронта

Создать документ DEV_BRIEF.md, который содержит:

DEV_EMAIL / DEV_PASSWORD:

cleaner

manager

примеры получения token

базовые curl-запросы:

today jobs

job detail

check-in / check-out

photos upload

PDF download

типовые ошибки API и их смысл

Цель:

чтобы фронт / мобильное приложение могли подключиться без вопросов

9. Правила работы в новом чате (ОБЯЗАТЕЛЬНО)

Всегда указывать путь файла в первой строке

Маленький файл → целиком

Большой файл → точечные патчи

Не повторять DONE

Явно помечать:

DONE

TODO

При команде «бриф»:

дополнять этот Master Brief

фиксировать прогресс

Итог

Этот документ — единая точка правды по проекту.
Новый чат продолжает этот же проект, без смены контекста.