MASTER BRIEF — Cleaning SaaS (MVP)

(актуальная версия для продолжения работы)

1. Что это за проект

Cleaning SaaS — backend-first SaaS для клининговых компаний (рынок UAE).

Проект покрывает реальный операционный цикл:

Планирование → Исполнение → Контроль → Отчёт

Фокус MVP

дисциплина и прозрачность работы клинеров

контроль выполнения через:

GPS

чек-листы

фото before / after

PDF-отчёт как финальный, юридически понятный артефакт работы

Проект не про биллинг, маркетинг или CRM.
Только операционка.

2. Архитектура (ЗАФИКСИРОВАНА)
Backend

Django (API-first)

DRF — только как API-слой

Auth: TokenAuthentication

БД:

SQLite (локально)

схема спроектирована под PostgreSQL

Асинхронщины нет

Очередей нет

Всё синхронно, прозрачно и детерминировано

Роли

Manager

Cleaner

3. Структура проекта (фактически есть)
backend/
├── apps/
│   ├── accounts/
│   │   └── models.py        # User, Company, roles
│   ├── api/
│   │   ├── views.py         # DRF API (login, jobs, checklist, photos, pdf)
│   │   ├── urls.py
│   │   ├── serializers.py
│   │   └── pdf.py           # генерация PDF отчёта
│   ├── jobs/
│   │   ├── models.py        # Job, JobChecklistItem, JobCheckEvent, File, JobPhoto
│   │   ├── utils.py         # distance_m, extract_exif_data
│   │   ├── migrations/
│   │   └── admin.py
│   └── locations/
│       └── models.py        # Location, ChecklistTemplate, ChecklistTemplateItem
├── config/
│   └── urls.py
├── requirements.txt
└── manage.py

Важные правила

apps.api — единственный API-слой

apps.jobs.views — legacy, не расширять

Новая логика → apps.api

Passing tests = истина

4. Что реализовано (DONE)
✅ Аутентификация

POST /api/auth/login/

email + password

Token-based auth

логин только для active cleaner

проверено через curl и mobile

✅ Jobs (Cleaner flow)

GET /api/jobs/today/

GET /api/jobs/<id>/

Статусы job:

scheduled

in_progress

completed

⚠️ Фактический контракт /api/jobs/today/ (ВАЖНО):

[
  {
    "id": 5,
    "location__name": "Dubai Marina Tower",
    "scheduled_date": "2026-01-17",
    "scheduled_start_time": null,
    "scheduled_end_time": null,
    "status": "scheduled"
  }
]


Контракт плоский, без вложенного location.
Mobile и frontend подстроены под это.

✅ Check-in / Check-out

POST /api/jobs/<id>/check-in/

POST /api/jobs/<id>/check-out/

Логика:

GPS проверка (≤ 100 м)

проверка роли

проверка владельца job

корректная смена статусов

создание JobCheckEvent

✅ Checklist

snapshot чек-листа при создании job

JobChecklistItem привязан к job

bulk update чек-листа

защита:

только назначенный клинер

только когда job = in_progress

required-пункты проверяются при check-out

✅ Photos — Backend полностью готов (Phase 9: BACKEND DONE)
Endpoints

GET /api/jobs/<id>/photos/

POST /api/jobs/<id>/photos/

DELETE /api/jobs/<id>/photos/<photo_type>/

Правила

ровно 2 фото на job:

before

after

загружает только клинер

только когда job = in_progress

after нельзя загрузить без before

нельзя удалить before, если уже есть after

Хранилище
/media/company/<company_id>/jobs/<job_id>/photos/<type>/<uuid>.<ext>

EXIF

извлекаются:

latitude

longitude

timestamp

если EXIF есть:

проверка расстояния до location (≤ 100 м)

если EXIF нет:

upload разрешён

exif_missing = true в ответе

Tests

python manage.py test — OK

покрыты:

upload

правила before → after

статусные ограничения

system check — OK

миграции применены

✅ PDF отчёт

POST /api/jobs/<id>/report/pdf/

генерация через ReportLab

PDF содержит:

job

location

cleaner

scheduled / actual timestamps

checklist

audit events

фото готовы к включению в следующей итерации PDF

5. Mobile Cleaner App — ТЕКУЩИЙ СТАТУС
Технологии

Expo

React Native

TypeScript

React Navigation (Native Stack)

Что работает (ФАКТ)

Login

Today Jobs

Job Details

Check-in / Check-out

Реальная интеграция с backend

Token хранится in-memory

Все JSON-запросы работают с:

Authorization: Token <token>

❌ ТЕКУЩАЯ ПРОБЛЕМА (КЛЮЧЕВАЯ, НЕ РЕШЕНА)
Photo upload (Mobile)

При загрузке фото:

используется FormData

backend endpoint корректный

photo_type передаётся

файл выбирается через expo-image-picker

Ошибки, которые сейчас возникают:

Cannot read property 'split' of undefined


(вероятно fileUri / asset.uri undefined)

Authentication credentials were not provided


→ Токен не доходит в multipart-запросе

Иногда 400 без detail (FormData + headers)

⚠️ Важно:
Все остальные запросы (JSON) успешно авторизуются.
Проблема строго в upload фото (FormData + auth).

6. Manager Portal (dubai-control) — ЗАФИКСИРОВАНО

React + Vite

TypeScript

Tailwind

shadcn/ui

API-first

Статус:

Manager Portal MVP визуально и архитектурно завершён.

7. Принцип работы с Lovable (ЗАФИКСИРОВАНО)

Lovable используется как:

генератор эталонного UI

источник UX-референсов

Правила:

Lovable не источник истины

API и логика — только репозиторий

Используется для:

новых экранов

крупных UI-итераций

8. Что НЕЛЬЗЯ делать ❌

Запрещено:

переписывать auth

переписывать jobs

переписывать check-in / check-out

менять модели

трогать миграции

предлагать Celery / async / очереди

«рефакторить ради красоты»

Любые изменения — только точечно и проверяемо.

9. Что делать в НОВОМ ЧАТЕ (TODO)

Фокус строго на одном:

🎯 Починить Photo Upload (Mobile)

Задачи:

Проверить expo-image-picker:

правильное получение asset.uri

убрать deprecated MediaTypeOptions

Гарантировать:

fileUri не undefined

Проверить apiFetch:

передаётся ли Authorization в multipart/form-data

Довести upload до:

успешного сохранения фото

появления фото в admin

После этого:

зафиксировать Phase 9: Photos — DONE

Итог

Этот документ — единая точка правды проекта.
Новый чат продолжается без смены контекста.