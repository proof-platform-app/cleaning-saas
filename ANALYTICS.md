# Analytics Semantics — Cleaning SaaS

Документ описывает **семантику аналитики**:  
что считать, зачем, и на каком этапе это имеет смысл внедрять.

⚠️ Это **НЕ часть MVP**.  
Файл нужен, чтобы:
- не ломать фундамент данных,
- заранее понимать, какие метрики возможны,
- не городить костыли при будущем росте продукта.

---

## 1. Цель аналитики

Аналитика предназначена для менеджера клининговой компании и отвечает на вопросы:

- Кто из клинеров работает **надёжно**, а кто — нет?
- Кто **опаздывает**, кто закрывает job слишком быстро или слишком долго?
- Где чаще всего возникают **проблемы с доказательствами работы**?
- Какие локации или типы объектов создают сложности?
- Как улучшить операционную дисциплину без микроменеджмента?

Аналитика **не про контроль ради контроля**, а про:
- качество,
- предсказуемость,
- масштабируемость бизнеса.

---

## 2. Фундамент данных (уже есть)

Все будущие метрики строятся **поверх существующих сущностей**:

### Job
- `status` (scheduled / in_progress / completed)
- `scheduled_date`
- `scheduled_start_time`, `scheduled_end_time`
- `actual_start_time`, `actual_end_time`
- `cleaner`
- `location`

### JobCheckEvent
- `event_type` (check_in / check_out)
- `created_at`
- `latitude`, `longitude`
- `distance_m` (до локации)

### JobChecklistItem
- `is_required`
- `is_completed`

### JobPhoto
- `photo_type` (before / after)
- наличие / отсутствие фото
- EXIF-данные (координаты, timestamp, если есть)

📌 **Важно:**  
Никакие дополнительные поля для аналитики в MVP не добавляются.  
Всё считается из фактов.

---

## 3. Планируемые метрики (семантика)

### 3.1 On-time performance

Что смотрим:
- насколько клинеры приходят вовремя.

Как считаем:
- разница между `scheduled_start_time` и `check_in.created_at`.

Метрики:
- % job’ов без опозданий;
- среднее опоздание (в минутах);
- систематические опоздания по клинеру.

---

### 3.2 Proof of Work completeness

Что смотрим:
- насколько job задокументирован.

Job считается **полностью подтверждённым**, если:
- есть `before_photo`,
- есть `after_photo`,
- все required пункты чек-листа выполнены,
- есть `check_out`.

Метрики:
- % job’ов с полным proof;
- % job’ов с пропущенными фото;
- % job’ов с незакрытым чек-листом.

---

### 3.3 Cleaner performance

По каждому клинеру:
- количество job’ов за период;
- % completed;
- % job’ов с полным proof;
- средняя длительность job;
- количество проблемных job’ов.

Цель:
- выявлять надёжных клинеров,
- находить зоны для обучения или контроля.

---

### 3.4 Duration & productivity

Что смотрим:
- фактическое время выполнения job.

Как считаем:
- `actual_end_time - actual_start_time`
  или разница между check-in / check-out событиями.

Анализ:
- job слишком короткий → риск “сделали формально”;
- job сильно дольше плана → сложная локация или перегруз.

---

### 3.5 Location insights

По локациям:
- количество job’ов;
- средняя длительность;
- % job’ов с проблемами;
- частые отклонения по GPS.

Цель:
- выявить проблемные объекты;
- лучше планировать время и ресурсы.

---

### 3.6 Daily operations (оперативная аналитика)

Для выбранного дня:
- сколько job’ов scheduled / in_progress / completed;
- сколько job’ов без proof;
- какие клинеры сейчас “в поле”.

Это основа для будущего **Operations Dashboard**.

---

## 4. Этапы внедрения аналитики

### Этап A — Backend-only (без UI)

Условия:
- Mobile Cleaner App стабилен;
- Manager Job Planning работает.

Что делаем:
- внутренние сервисы расчёта метрик;
- тесты на корректность вычислений;
- никаких новых моделей.

---

### Этап B — Summary API для менеджера

Добавляются агрегированные эндпоинты, например:
- `GET /api/manager/analytics/summary`
- `GET /api/manager/analytics/cleaners`

Возвращают:
- totals,
- percentages,
- averages.

Без сырых данных.

---

### Этап C — Analytics UI

Только при наличии реальных данных (1–3 месяца):

- отдельная страница Analytics в Manager Portal;
- фильтры: период / клинер / локация;
- 2–3 ключевых графика + таблицы.

---

## 5. Принципы и ограничения

❌ Не добавлять аналитические флаги в Job  
❌ Не усложнять модели ради отчётов  
❌ Не делать аналитику до появления реального usage  

✅ Считать всё из фактов  
✅ Сначала backend → потом API → потом UI  
✅ Analytics = инструмент улучшения, не давления  

---

## 6. Связь с MVP

На текущем этапе:
- Analytics **не реализуется**;
- этот файл — ориентир на будущее;
- текущая архитектура полностью совместима с этим планом.
